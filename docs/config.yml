backend:
  name: git-gateway
  branch: main   # or whatever your default branch is

media_folder: "docs/assets/uploads"
public_folder: "/assets/uploads"

collections:
  - name: "logs"
    label: "Logs"
    folder: "docs/logs"
    create: true
    slug: "{{year}}-{{month}}-{{day}}"
    extension: "md"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Date", name: "date", widget: "datetime" }
      - { label: "Tasks Completed", name: "tasks", widget: "markdown" }
      - { label: "Issues Encountered", name: "issues", widget: "markdown" }
      - { label: "Solutions / Workarounds", name: "solutions", widget: "markdown" }
      - { label: "Next Steps", name: "next", widget: "markdown" }
      - { label: "Notes & Reflections", name: "notes", widget: "markdown" }
